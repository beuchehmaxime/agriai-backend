//data must be in lowercase
// Crops with a trained model in the ML service. Other crops are still accepted
// and diagnosed by Claude from the image.
export const SelectedCropsData: string[] = ["tomato"];
